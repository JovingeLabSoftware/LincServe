var config = {
	'prod': {
 	    'version': 1.0,
  		'couchdb': {
			'ip': '127.0.0.1',
			'bucket': 'LINCS1',
			'port': 8093,
			'password': null
		}
	},
	'devel': {
 	    'version': 1.0,
  		'couchdb': {
			'ip': '127.0.0.1',
			'bucket': 'LINCS1',
			'port': 8093,
			'password': null
		}
	}
};

if(process.env.LINCS_DEVEL){
	module.exports = config.devel;
} else {
	module.exports = config.prod;	
}
